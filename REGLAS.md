# REGLAS APRENDIDAS — Plomero Culiacán

Reglas duras extraídas del historial de commits. Cada línea documenta un error real ya cometido para que un agente NO lo repita.

- [2026-06-11] JS/MINIFICACIÓN: tras minificar main.js, verificar que las URLs `wa.me` no queden truncadas — una URL cortada genera un SyntaxError fatal que mata menú móvil, formularios y tracking en TODO el sitio — por qué (commit f8c72299). Severidad: alta.
- [2026-06-11] CACHÉ: al cambiar CSS **o JS (main.js)**, versionar la URL (`?v=AAAAMMDD`) en TODAS las páginas que lo referencian Y subir CACHE_NAME del service worker (sw.js) + versionar el asset en PRECACHE_ASSETS — ambos se sirven con Cache-Control immutable 1 año y el SW los precachea cache-first, así que sin versionar los visitantes recurrentes nunca reciben el fix. main.js estaba referenciado SIN `?v=` pese a estar la regla para CSS — por qué (commits a03a6b2b, mant-altos-20260612). Severidad: alta.
- [2026-06-11] MÓVIL: las tablas (precios, etc.) deben tener scroll horizontal contenido — patrón establecido: envolver `<table>` en `<div class="table-wrapper">` con `.table-wrapper{overflow-x:auto}` (regla en los 3 CSS); sin eso la columna derecha se sale del viewport en /, /precios/ y posts del blog — por qué (commits f44ef39f, mant-altos-20260612). Severidad: alta.
- [2026-06-11] MÓVIL: toda imagen debe llevar `max-width:100%` (ej. .google-review-image, .before-after-image) — sin regla CSS renderizan a 1200px y desbordan en móvil — por qué (commit f44ef39f). Severidad: media.
- [2026-06-11] MÓVIL: no usar grids con columnas fijas inline (ej. benefits-grid 2 columnas) — desbordan a 375px; usar `auto-fit minmax(260px,1fr)` — por qué (commit f44ef39f). Severidad: media.
- [2026-06-11] MÓVIL/A11Y: los tap targets deben medir ~44px (breadcrumbs tenían 32x22px); ampliar área táctil con padding + margen negativo sin cambiar el aspecto visual — por qué (commit f44ef39f). Severidad: media.
- [2026-06-11] CSS/PARIDAD: aplicar cada fix de estilos en los TRES archivos (styles.css, styles.min.css y styles.7f293647.css servido) — una regla añadida solo en uno (.footer-logo img) deja el sitio inconsistente — por qué (commit f44ef39f). REINCIDIÓ 2026-06-12: el fix de tap targets de breadcrumbs faltaba en styles.css (solo estaba en min y hash). REINCIDIÓ 2026-06-12 tarde (movil-203): el bloque fallback global `@media(max-width:768px){table{display:block;overflow-x:auto;-webkit-overflow-scrolling:touch}}` faltaba en styles.css (estaba en min/hash). NOTA: styles.css es SOLO fuente — ninguna página lo sirve (sirven styles.min.css o styles.7f293647.css), así que arreglar la paridad en styles.css NO cambia ningún asset servido y NO requiere bump de ?v= ni sw.js; solo verificar paridad `tr -d ' \n' | grep -oc <regla>` = 1/1/1. Severidad: media.
- [2026-06-05] MÓVIL: los grid items que deben encogerse llevan `min-width:0`, no `auto` — con min-width:auto las columnas no se encogen y empujan contenido (.stats-grid) fuera de pantalla en ≤360px — por qué (commit bdf53978). Severidad: media.
- [2026-06-11] SEO: nunca crear cientos de páginas casi idénticas (colonias eran plantillas 80-86% iguales); consolidar en pocas páginas de zona con 301 — son doorways invisibles para Google y peso muerto de rastreo — por qué (commit 320950bc). Severidad: alta.
- [2026-06-11] SEO: cada página local debe tener coordenadas GPS reales y únicas, no la coordenada genérica del centro repetida — la geo duplicada es señal de doorway — por qué (commit 0a865c93). Severidad: media.
- [2026-06-09] SEO/SCHEMA: NO poner aggregateRating/review auto-servidos en blog ni páginas sin negocio real — viola la política self-serving reviews de Google; mantener reviews solo en páginas de negocio — por qué (commit 08a95902). Severidad: alta.
- [2026-06-08] SEO: verificar que `og:image` y `twitter:image` apunten a un archivo que EXISTE — apuntaban a archivos inexistentes en varias páginas — por qué (commits 590d3e4a, f8c72299, 26cf9939). Severidad: media.
- [2026-06-11] ENLACES: al consolidar/eliminar páginas, revisar y reparar rutas de imagen inválidas (`../../../`) y enlaces internos rotos generados por el cambio de profundidad — por qué (commits 8a747e6e, f8c72299). Severidad: alta.
- [2026-06-11] IMÁGENES: no referenciar PNG inexistentes; usar los WebP que sí existen en el repo — había 18 rutas a PNG inexistentes — por qué (commit f8c72299). Severidad: media.
- [2026-06-11] CONTENIDO: el email de contacto correcto es `info@plomeroculiacanpro.mx`, NO `info@plomeropro.com` — estaba mal en 14 páginas — por qué (commit f8c72299). Severidad: media.
- [2026-06-11] SEO/ENLACES: al borrar o consolidar páginas, verificar CERO enlaces internos a las páginas borradas y actualizar sitemap (404, home, hubs, meta-refresh) — por qué (commits 320950bc, 8a747e6e). Severidad: alta.
- [2026-06-11] SEO: las páginas off-topic (ej. marcha-paz) van con noindex y fuera del sitemap — no diluir el sitio temático con contenido ajeno — por qué (commits f8c72299, 26cf9939). Severidad: baja.
- [2026-06-09] REDIRECTS: todo redirect debe apuntar a un destino que existe — uno apuntaba a `/2026/` inexistente y se corrigió a `/2025/` — por qué (commit 26cf9939). Severidad: media.
- [2026-06-03] RENDIMIENTO/CLS: las imágenes (sobre todo el logo) deben declarar `width` y `height` para prevenir CLS — faltaban en 22 páginas — por qué (commit bd9ccadf). Severidad: media.
- [2026-06-03] PWA: el `theme_color`/`theme-color` debe ser el color de marca (#F97316), no un placeholder (#0066cc), y debe existir en cada página — por qué (commits bd9ccadf, fdc89c6c). Severidad: baja.
- [2026-05-20] RENDIMIENTO: optimizar imágenes pesadas a WebP antes de servirlas — el logo pasó de 101KB a 6KB (-94%) — por qué (commit 0346f9eb). Severidad: media.
- [2026-06-03] SEO: cada página indexable nueva debe quedar incluida en el sitemap (incluyendo priority_sitemap.xml) — faltaban páginas indexables — por qué (commits e7899c86, f8c72299). Severidad: media.
- [2026-06-12] SEO/SCHEMA: el og:url y TODAS las URLs propias dentro del JSON-LD (url del LocalBusiness, item del BreadcrumbList) deben coincidir EXACTAMENTE con el canonical de la página — había og:url apuntando a la home (2 páginas), URLs sin el prefijo /servicios/ que daban 404 (2 páginas) y un typo de año; al crear/editar una página, grep de plomeroculiacanpro.mx en el head y comparar contra el canonical — por qué (corrida noche 20260612, seo-201/202/203/204/205). REINCIDIÓ 2026-06-12 tarde (seo-206/207): og:url apuntaba a la home en instalacion-de-boiler y plomero-cerca-de-mi (eran páginas indexables); patrón frecuente en la plantilla de servicio — al editar cualquier servicio, verificar `grep 'og:url' index.html` == canonical. REINCIDIÓ 2026-06-12 noche (seo-301/302/303): variante distinta — el `BreadcrumbList` del JSON-LD quedó TRUNCADO a un solo `itemListElement` (pos 1 "Inicio" → home), de modo que el último item apuntaba a la home y NO al canonical, en 3 páginas de servicio indexables (instalacion-de-boiler, plomero-a-domicilio, plomero-cerca-de-mi). El patrón correcto es 3 niveles: Inicio → /, Servicios → /servicios/, [Página] → canonical. Chequeo: el ÚLTIMO `item` del BreadcrumbList debe == canonical; comprobar también que existan los 3 niveles (`grep -c '"position": 3' index.html` == 1 en páginas de servicio). REINCIDIÓ 2026-06-13 (seo-304, desazolve-de-drenajes): VARIANTE que el chequeo de "último item==canonical" NO atrapa — el BreadcrumbList quedó en 2 niveles (pos 1 Inicio→/ , pos 2 →la propia página) OMITIENDO el nivel intermedio "Servicios"; como el último item igual coincidía con el canonical, solo el chequeo de "existen posiciones 1, 2 Y 3" lo delata. AUTOMATIZADO: este chequeo (y seo-201..207) ahora los ejecuta de forma determinista el revisor `revisor-indexabilidad` (`python3 .pipeline/check-indexabilidad.py`), 7º revisor del pipeline; corre en cada mantenimiento sin depender de red. Severidad: media.
- [2026-06-12] PERF/LCP: solo UNA imagen por página lleva fetchpriority="high" (el hero LCP); el logo del nav y las imágenes bajo el fold van sin fetchpriority y con loading="lazy" — 3 páginas de servicio tenían 2-3 imágenes en high compitiendo contra el LCP — por qué (corrida noche 20260612, perf-202/203/204). Severidad: media.
- [2026-06-12] CSS/CASCADA: al añadir reglas al final de los CSS compartidos, verificar que ningún selector de la regla nueva pise propiedades de reglas anteriores de igual especificidad — un selector .hero-phone-link con display:inline-block al final del archivo habría anulado el display:none de escritorio y mostrado el enlace en desktop; tras añadir CSS, medir con Chrome headless (display/getBoundingClientRect) en móvil Y desktop — por qué (corrida noche 20260612, movil-201, detectado antes de publicar). Severidad: media.
- [2026-06-12] PERF/LCP: la plantilla de páginas de servicio y blog emite la SEGUNDA imagen de contenido (img#2, bajo el fold, tras `<header class="hero">`) con `loading="eager"` — debe ir `loading="lazy"` para no competir con la descarga del hero LCP. Reincidió en 14 páginas (perf-301..314). Distinguir del hero (lleva `fetchpriority="high"`, no se toca) y del logo del nav. Regla segura: cambiar `loading="eager"`→`"lazy"` solo en imgs SIN `fetchpriority="high"` y cuyo src no contenga "logo". Severidad: media.
- [2026-06-12] INFRA/PUSH: el hook `.git/hooks/pre-push` (auto-indexación Google) invoca `node` sin ruta absoluta; en este entorno `node` vive en `/usr/local/bin` y NO está en el PATH del shell por defecto, así que un `git push` pelado falla con `node: command not found` (exit 127) y ABORTA el push. Solución usada: `PATH="/usr/local/bin:$PATH" git push` — así el hook corre y encola/envía las URLs. Distinto de infra-001 (aquel era la ruta de sites.json). Pendiente menor (infra-002): endurecer el hook para que resuelva node por sí mismo. Severidad: baja.
- [2026-06-13] A11Y/PLANTILLA: el modal exit-intent (`<div id="exit-intent-popup">`) debe llevar `role="dialog" aria-modal="true" aria-labelledby="exit-popup-title"` y su `<h3>` "¡Espera! 🛠️" debe tener `id="exit-popup-title"` — igual que index.html (la versión correcta). La plantilla de páginas de SERVICIO emitió el popup SIN esos atributos en al menos 5 páginas (cerca-de-mi, desazolve, a-domicilio, economico, instalacion-de-boiler), así que un lector de pantalla no lo anuncia como diálogo modal — por qué (corrida 20260613-2000, a11y-302). Chequeo al crear/editar una página de servicio: `grep -c 'id="exit-intent-popup" role="dialog"' index.html` == 1. Severidad: media.
- [2026-06-14] MÓVIL/PLANTILLA: los botones flotantes de conversión (`.floating-btn`/`.floating-call`/`.floating-whatsapp`) se estilan con CSS INLINE por página (NO están en los 3 CSS compartidos: 0 ocurrencias). Si una página usa `class="floating-btn ..."` en el HTML pero su `<style>` inline NO trae esas reglas, los botones de WhatsApp/llamada caen a `position:static` (al fondo del documento, no flotan) y colapsan a <44px — los 2 CTA principales rotos en móvil. Caso real (movil-401): `servicios/reparacion-de-boiler/index.html` era la única página con los botones en el HTML pero sin las reglas en su `<style>` (tiene un bloque de estilos propio distinto al resto). Chequeo al crear/editar una página: si `grep -c 'class="floating-btn' index.html` ≥1, entonces `grep -c '.floating-btn{' index.html` debe ser ≥1. Fix: copiar verbatim las 4 reglas (`.floating-btn`, `.floating-btn:hover`, `.floating-call`, `.floating-whatsapp`) de cualquier página de servicio correcta (p.ej. plomero-economico). Solo `<style>` inline → sin bump de `?v=`/sw.js. Severidad: alta (CTA de conversión rotos). NOTA pendiente: a largo plazo conviene mover `.floating-btn` a los 3 CSS compartidos (con paridad) para eliminar la duplicación por página (fuera de alcance auto: requiere bump ?v=+sw.js y validación visual).
- [2026-06-14] SEO/ENLACES: los enlaces internos deben usar la forma de directorio (`/servicios/<slug>/`) que coincide con el `canonical`, NO la forma `/servicios/<slug>/index.html`. Caso real (seo-401): 59 enlaces (incluida la rejilla de servicios de la home, la página de mayor autoridad) apuntaban a `…/index.html` mientras 535+ ya usaban la forma de directorio y el canonical de cada página es siempre la de directorio → segunda URL rastreable del mismo contenido (ruta de rastreo duplicada) contradiciendo el canonical desde los enlaces internos. El checker de links no lo marca porque el destino físico existe (sirve 200). Fix mecánico: `perl -i -pe 's{(href="\.?/servicios/[a-z-]+/)index\.html"}{$1"}g'`. Solo HTML → sin bump. Severidad: media.
- [2026-06-14] CONTENIDO/AÑO: los títulos de las tarjetas de "artículo relacionado" embeben el año a mano y se desincronizan del título real del post (mismo post mostrado como "Guía de Compra 2024" / "2025" / "2026" en distintas páginas). Caso real (cont-003): la tarjeta del post de tinaco (título real "[2026]") aparecía como 2025 en la home y 2024 en 5 servicios. El `datetime=` de publicación NO se toca (es la fecha real de publicación); solo el año del TÍTULO visible de la tarjeta. Unificar al año del título real del post. Ampliación del patrón "año caduco en title/h1" (que ya cubre el checker en title/h1, pero NO en el texto de tarjetas de relacionados). Severidad: media.
- [2026-06-16] MÓVIL/TAP-TARGET: los enlaces inline dentro de tablas (p.ej. `.service-link` en la tabla de precios de `/precios/`) rinden a la altura de la línea (~41px) y quedan bajo el mínimo táctil de 44px en móvil — un `<a>` inline NO recibe padding vertical efectivo. Caso real (movil-501): los 25 `.service-link` de la tabla de precios (selector solo en `<style>` inline, no en los 3 CSS) medían 41px en 375px. Fix mecánico seguro: `display:inline-block; padding:0.35rem 0` en el selector del enlace (alcanza ≥44px sin desbordar la tabla en móvil ni desktop). Verificar headless en 375px (`getBoundingClientRect().height>=44` en todos) Y 1280px (sin overflow). Amplía el patrón de tap targets de breadcrumbs ([2026-06-11]) a enlaces de tabla. Solo `<style>` inline → sin bump de `?v=`/sw.js. Severidad: media.
- [2026-06-17] SEO/ENLACES: al CONSOLIDAR/borrar páginas, los enlaces internos a las páginas borradas suelen existir en MÚLTIPLES formas DENTRO de la MISMA página, y una reescritura parcial deja unas rotas y otras no. Caso real (links-201): la consolidación humana de 32 colonias→hubs (commit eaf83781) reescribió la lista `<ul>` de colonias de `servicios/plomero-colonias-culiacan/index.html` (forma ABSOLUTA `/servicios/.../`, apunta a páginas existentes → check NO la marca) pero OMITIÓ la rejilla de tarjetas `<a class="card" href="./slug/">` (forma RELATIVA `./slug/` → 25 enlaces a directorios borrados). check-plantilla.py SÍ atrapó las 25 relativas (categoría links, ALTA). Fix mecánico: reescribir cada `href="./slug/"` roto al destino 301 EXACTO ya definido en `_redirects` (el hub de zona, que existe), NO a la URL 301'd (regla seo-401: enlazar al destino canónico, no vía redirect). Chequeo al consolidar: `python3 .pipeline/check-plantilla.py` debe dar 0 enlaces rotos Y revisar que NINGUNA página conserve `href="./<slug-borrado>/"` en ninguna de sus listas/rejillas. Severidad: alta. NOTA relacionada (cont-010/011, pendiente humano): el GENERADOR de la lista `<li>` truncó "Culiacán" del nombre visible de colonias que lo contienen → anclas vacías ("Plomero en ") o truncadas ("Plomero en Nuevo"); al autogenerar texto de ancla desde nombres de colonia, no recortar el sufijo "Culiacán".
- [2026-06-13] PROD/JS-RUNTIME: el health-check del pipeline valida `main.js` con `node --check` (solo SINTAXIS); eso NO atrapa errores de RUNTIME que sí rompen en el navegador. Caso real (prod-001): producción lanzaba `TypeError: (intermediate value)(...) is not a function` en `main.js?v=20260612:273:3` en cada carga de `/`, `/precios/`, `/contacto/` — un `()` espurio que invoca el retorno de `requestIdleCallback`/`setTimeout` (un id numérico) como si fuera función. El syntax check pasaba; solo Chrome headless lo veía. REGLA: validar el JS de producción cargando las páginas clave en Chrome headless y capturando `pageerror`/`console.error` (no solo `node --check` local). AUTOMATIZADO: lo ejecuta el 8º revisor `revisor-produccion` (`node .pipeline/check-produccion.mjs`), que además verifica uptime 200, headers de seguridad, mixed content y el formulario, todo contra https://plomeroculiacanpro.mx en cada mantenimiento. Severidad: alta (cualquier excepción JS no capturada).
