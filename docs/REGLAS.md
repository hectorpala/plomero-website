# REGLAS APRENDIDAS — Plomero Culiacán

Reglas duras extraídas del historial de commits. Cada línea documenta un error real ya cometido para que un agente NO lo repita.

> PRESUPUESTO (se inyecta en CADA corrida y lo lee cada subagente — mantenlo curado): regla = 1-2 líneas accionables (qué hacer + el checker que lo caza). El relato de cada incidente vive en HISTORIAL.jsonl, NO aquí. Cuando un error ya está mecanizado en un checker, la regla se reduce a «qué + checker». Corre `python3 .pipeline/check-reglas.py` y consolida si excede el presupuesto.

- [2026-06-11] JS/MINIFICACIÓN: tras minificar main.js, verificar que las URLs `wa.me` no queden truncadas — una URL cortada genera un SyntaxError fatal que mata menú móvil, formularios y tracking en TODO el sitio — por qué (commit f8c72299). Severidad: alta.
- [2026-06-11] CACHÉ: al cambiar CSS **o JS (main.js)**, versionar la URL (`?v=AAAAMMDD`) en TODAS las páginas que lo referencian Y subir CACHE_NAME del service worker (sw.js) + versionar el asset en PRECACHE_ASSETS — ambos se sirven con Cache-Control immutable 1 año y el SW los precachea cache-first, así que sin versionar los visitantes recurrentes nunca reciben el fix. main.js estaba referenciado SIN `?v=` pese a estar la regla para CSS — por qué (commits a03a6b2b, mant-altos-20260612). Severidad: alta.
- [2026-06-11] MÓVIL: las tablas (precios, etc.) deben tener scroll horizontal contenido — patrón establecido: envolver `<table>` en `<div class="table-wrapper">` con `.table-wrapper{overflow-x:auto}` (regla en los 3 CSS); sin eso la columna derecha se sale del viewport en /, /precios/ y posts del blog — por qué (commits f44ef39f, mant-altos-20260612). Severidad: alta.
- [2026-06-11] MÓVIL: toda imagen debe llevar `max-width:100%` (ej. .google-review-image, .before-after-image) — sin regla CSS renderizan a 1200px y desbordan en móvil — por qué (commit f44ef39f). Severidad: media.
- [2026-06-11] MÓVIL: no usar grids con columnas fijas inline (ej. benefits-grid 2 columnas) — desbordan a 375px; usar `auto-fit minmax(260px,1fr)` — por qué (commit f44ef39f). Severidad: media.
- [2026-06-11] MÓVIL/A11Y: los tap targets deben medir ~44px (breadcrumbs tenían 32x22px); ampliar área táctil con padding + margen negativo sin cambiar el aspecto visual — por qué (commit f44ef39f). Severidad: media.
- [2026-06-11] CSS/PARIDAD: todo fix de estilos va en los TRES archivos (styles.css, styles.min.css, styles.7f293647.css). styles.css es SOLO fuente — ninguna página lo sirve — así que tocarlo NO requiere bump de ?v=/sw.js; solo verificar paridad `tr -d ' \n' | grep -oc <regla>` = 1/1/1. Reincidió 2× (detalle en HISTORIAL). Severidad: media.
- [2026-06-05] MÓVIL: los grid items que deben encogerse llevan `min-width:0`, no `auto` — con min-width:auto las columnas no se encogen y empujan contenido (.stats-grid) fuera de pantalla en ≤360px — por qué (commit bdf53978). Severidad: media.
- [2026-06-11] SEO: nunca crear cientos de páginas casi idénticas (colonias eran plantillas 80-86% iguales); consolidar en pocas páginas de zona con 301 — son doorways invisibles para Google y peso muerto de rastreo — por qué (commit 320950bc). Severidad: alta.
- [2026-06-11] SEO: cada página local debe tener coordenadas GPS reales y únicas, no la coordenada genérica del centro repetida — la geo duplicada es señal de doorway — por qué (commit 0a865c93). Severidad: media.
- [2026-06-09] SEO/SCHEMA: NO poner aggregateRating/review auto-servidos en blog ni páginas sin negocio real — viola la política self-serving reviews de Google; mantener reviews solo en páginas de negocio — por qué (commit 08a95902). Severidad: alta.
- [2026-06-08] SEO: verificar que `og:image` y `twitter:image` apunten a un archivo que EXISTE — apuntaban a archivos inexistentes en varias páginas — por qué (commits 590d3e4a, f8c72299, 26cf9939). Severidad: media.
- [2026-06-11] ENLACES: al consolidar/eliminar páginas, revisar y reparar rutas de imagen inválidas (`../../../`) y enlaces internos rotos generados por el cambio de profundidad — por qué (commits 8a747e6e, f8c72299). Severidad: alta.
- [2026-06-11] IMÁGENES: no referenciar PNG inexistentes; usar los WebP que sí existen en el repo — había 18 rutas a PNG inexistentes — por qué (commit f8c72299). Severidad: media.
- [2026-06-11] CONTENIDO: el email de contacto correcto es `info@plomeroculiacanpro.mx`, NO `info@plomeropro.com` — estaba mal en 14 páginas — por qué (commit f8c72299). Severidad: media.
- [2026-06-11] SEO/ENLACES: al borrar o consolidar páginas, verificar CERO enlaces internos a las páginas borradas y actualizar sitemap (404, home, hubs, meta-refresh) — por qué (commits 320950bc, 8a747e6e). Severidad: alta.
- [2026-06-11] SEO: las páginas off-topic (ej. marcha-paz) van con noindex y fuera del sitemap — no diluir el sitio temático con contenido ajeno — por qué (commits f8c72299, 26cf9939). Severidad: baja.
- [2026-06-09] REDIRECTS: todo redirect debe apuntar a un destino que existe — uno apuntaba a `/2026/` inexistente y se corrigió a `/2025/` — por qué (commit 26cf9939). Severidad: media.
- [2026-06-03] RENDIMIENTO/CLS: las imágenes (sobre todo el logo) deben declarar `width` y `height` para prevenir CLS — faltaban en 22 páginas — por qué (commit bd9ccadf). Severidad: media.
- [2026-06-03] PWA: el `theme_color`/`theme-color` debe ser el color de marca (#F97316), no un placeholder (#0066cc), y debe existir en cada página — por qué (commits bd9ccadf, fdc89c6c). Severidad: baja.
- [2026-05-20] RENDIMIENTO: optimizar imágenes pesadas a WebP antes de servirlas — el logo pasó de 101KB a 6KB (-94%) — por qué (commit 0346f9eb). Severidad: media.
- [2026-06-03] SEO: cada página indexable nueva debe quedar incluida en el sitemap (incluyendo priority_sitemap.xml) — faltaban páginas indexables — por qué (commits e7899c86, f8c72299). Severidad: media.
- [2026-06-12] SEO/SCHEMA: og:url y todas las URLs propias del JSON-LD (LocalBusiness url, items del BreadcrumbList) == canonical. El BreadcrumbList de páginas de servicio = 3 niveles (Inicio→/, Servicios→/servicios/, [Página]→canonical) y el nivel intermedio debe ser el hub real `/servicios/`, NUNCA una ancla de la home (`BASE/#servicios`). AUTO en `check-indexabilidad.py` (último item==canonical + posiciones 1,2,3 + check 2b: ningún item intermedio = `BASE/#...`). Reemplazo masivo: el JSON-LD viene con y SIN espacios → guíate por el checker parse-aware, no por string exacto. Casos seo-201..304, breadcrumb-ancla-19 (HISTORIAL). Severidad: media.
- [2026-06-12] PERF/LCP: solo UNA imagen por página lleva fetchpriority="high" (el hero LCP); el logo del nav y las imágenes bajo el fold van sin fetchpriority y con loading="lazy" — 3 páginas de servicio tenían 2-3 imágenes en high compitiendo contra el LCP — por qué (corrida noche 20260612, perf-202/203/204). Severidad: media.
- [2026-06-12] CSS/CASCADA: al añadir reglas al final de los CSS compartidos, verificar que ningún selector de la regla nueva pise propiedades de reglas anteriores de igual especificidad — un selector .hero-phone-link con display:inline-block al final del archivo habría anulado el display:none de escritorio y mostrado el enlace en desktop; tras añadir CSS, medir con Chrome headless (display/getBoundingClientRect) en móvil Y desktop — por qué (corrida noche 20260612, movil-201, detectado antes de publicar). Severidad: media.
- [2026-06-12] PERF/LCP: la plantilla de páginas de servicio y blog emite la SEGUNDA imagen de contenido (img#2, bajo el fold, tras `<header class="hero">`) con `loading="eager"` — debe ir `loading="lazy"` para no competir con la descarga del hero LCP. Reincidió en 14 páginas (perf-301..314). Distinguir del hero (lleva `fetchpriority="high"`, no se toca) y del logo del nav. Regla segura: cambiar `loading="eager"`→`"lazy"` solo en imgs SIN `fetchpriority="high"` y cuyo src no contenga "logo". Severidad: media.
- [2026-06-12] INFRA/PUSH: `node` no está en el PATH del shell por defecto (vive en `/usr/local/bin`) → `git push` pelado aborta (exit 127) en el hook pre-push. Usar `PATH="/usr/local/bin:$PATH" git push`. Pendiente menor (infra-002): endurecer el hook. Severidad: baja.
- [2026-06-13] A11Y/PLANTILLA: el modal exit-intent (`<div id="exit-intent-popup">`) debe llevar `role="dialog" aria-modal="true" aria-labelledby="exit-popup-title"` y su `<h3>` con `id="exit-popup-title"` — igual que index.html. AUTO en `check-plantilla.py` check 5. Caso a11y-302 (HISTORIAL). Severidad: media.
- [2026-06-14] MÓVIL/PLANTILLA: los botones flotantes (`.floating-btn`…) se estilan con CSS INLINE por página. Si el HTML usa `class="floating-btn"` pero el `<style>` NO trae las reglas, los CTA caen a position:static y <44px (rotos en móvil). Chequeo: `grep -c 'class="floating-btn'` ≥1 → `grep -c '.floating-btn{'` ≥1. Fix: copiar las 4 reglas de una página sana (p.ej. plomero-economico); inline → sin bump. Severidad: alta (CTA rotos).
- [2026-06-14] SEO/ENLACES: enlaces internos en forma de directorio (`/servicios/<slug>/`) == canonical, NUNCA `…/index.html` (ruta de rastreo duplicada; el checker de links no la caza porque sirve 200). Fix: `perl -i -pe 's{(href="\.?/servicios/[a-z-]+/)index\.html"}{$1"}g'`. Solo HTML→sin bump. Caso seo-401 (HISTORIAL). Severidad: media.
- [2026-06-14] CONTENIDO/AÑO: el año caduco/desincronizado aparece donde el checker NO mira (title/h1 sí los cubre): tarjetas de "relacionados", badge "Actualizado", alt, FAQ, JSON-LD headline y CUERPO. Unificar TODO el año visible al del título real; `datePublished` NO se toca (fecha real), `dateModified`=hoy si editas. Casos cont-003/cont-014/cont-r03/cont-r04 (HISTORIAL). Severidad: media.
- [2026-06-16] MÓVIL/TAP-TARGET: un `<a>` inline (p.ej. `.service-link` en /precios/) rinde a la altura de línea (<44px) — el inline no recibe padding vertical efectivo. Fix por CONTEXTO (el padding NO es transferible): en TABLA `display:inline-block;padding:0.35rem 0` (celdas multilínea ya dan ~60px); en PROSA `<p>` usar `padding:0.6rem 0` (una línea solo llega a 38px con 0.35rem). Al arreglar una clase, medir TODAS sus instancias (`grep -c 'class="service-link"'`), no solo el contenedor principal. Verificar headless 375px (≥44px) y 1280px (sin overflow). Solo `<style>` inline → sin bump. Reincidió movil-502 (detalle en HISTORIAL). Severidad: media.
- [2026-06-17] SEO/ENLACES: al CONSOLIDAR/borrar páginas, los enlaces a las borradas suelen estar en MÚLTIPLES formas en la MISMA página (lista `<ul>` absoluta + rejilla `<a class="card" href="./slug/">` relativa); una reescritura parcial deja unas rotas. `check-plantilla.py` atrapa las relativas rotas (links, ALTA). Fix: reescribir cada `href` roto al destino que EXISTE (el hub 301 de `_redirects`), NO vía la URL redirigida (regla seo-401). Chequeo: `check-plantilla.py` 0 rotos Y ninguna página conserva `href="./<slug-borrado>/"`. Nota (cont-010/011, pendiente humano): al autogenerar anclas desde nombres de colonia, no recortar el sufijo "Culiacán". Severidad: alta.
- [2026-06-13] PROD/JS-RUNTIME: `node --check` valida solo SINTAXIS, no errores de RUNTIME que sí rompen en el navegador (caso prod-001: un `()` espurio invocando el id de setTimeout → TypeError en cada carga de /, /precios/, /contacto/). REGLA: validar el JS de producción cargando las páginas clave en Chrome headless y capturando `pageerror`/`console.error`. AUTOMATIZADO en `check-produccion.mjs` (revisor-produccion: + uptime 200, headers, mixed content, formulario). Severidad: alta.
- [2026-06-18] CRECIMIENTO/PLANTILLA: una página de servicio NUEVA debe llevar el `cuerpo_html` COMPLETO como las hermanas (ref: servicios/destape-de-drenajes): FAQ, cta-box, CTA final, `#sobre-nosotros`, `#contacto`, cross-sell; todos los `wa.me ?text=`/og:image/hero deben corresponder al servicio (sin fuga de "zona oriente"); NO inventar testimonios (seo-104). Chequeo: `grep -c href="tel:` ≥2, `id="contacto"` ==1; la fuga de zona la caza AUTO `check-plantilla.py`. Casos crece-001/plt-leak-zona (HISTORIAL). Severidad: media.
- [2026-06-20] INFRA/SENSOR: el dead-man's switch `check-infra.mjs` dispara ALTA falsa "verificación ciega" CADA día cuando algo cambia sin actualizar el sensor. Dos clases vistas: (1) cambió el patrón del log del driver → actualizar su regex (hoy `/^(run|auto-agente)-.*\.log$/`); (2) un checker NUEVO que es UTILIDAD (texto humano, requiere args, no es sensor de página) debe declarar el marcador `infra:utilidad-no-sensor` en su cabecera (primeros 800 chars) para auto-excluirse. Casos infra-003/005, css-paridad-marcador-20260620 (HISTORIAL). Severidad: baja, pero envenena la confianza en el sensor → confiabilidad alta.
- [2026-06-19] BLOG/PLANTILLA (AUTO en `check-plantilla.py`): en posts de blog — sin `<div>` de bloque dentro de `<h2>` (4c); sin `<section class="related-articles">` duplicada (4d); el `<header class="article-header">` NUNCA lleva `aria-hidden` (oculta fecha/lectura; solo el `.article-title-hidden` interior va oculto) (4e); los indexables traen `og:locale`/`og:site_name`/`twitter:url`=canonical (4f). Casos 20260619/20260623. Severidad: media.
- [2026-06-19] CSS/PARIDAD-TOTAL: las 3 hojas deben tener las MISMAS reglas (home+24 colonias sirven `styles.7f293647.css`; el resto `styles.min.css`; `styles.css`=fuente no servida). AUTO con `check-css-paridad.py` (standalone) + `check-plantilla.py::check_css_parity` (descompone cada CSS en átomos selector+declaraciones; ignora minificación/agrupación). Fix: unificar a `styles.min.css`; si cambia el render de home+colonias → bump `?v=` en esas 25 págs + `CACHE_NAME` sw.js. Los 6 `servicios/plomero/*` son redirects intencionales (no derivas). Detalle css-paridad-20260619 en HISTORIAL. Severidad: media.
- [2026-06-21] SISTEMA/VERIFICADOR: el subagente de la FASE 7 (verificar "¿quedó bien?") debe ser SOLO-LECTURA. JAMÁS darle Edit/Write ni dejarlo correr git checkout/commit/merge/push. Lanzarlo con un agentType de solo-lectura (Read/Grep/Glob/Bash-de-consulta) e instrucción explícita "NO modifiques archivos, NO toques git". Caso verifier-rogue: un verificador general-purpose implementó+commiteó+mergeó+PUSHEÓ a main sin autorización y editó una página; su veredicto queda INVALIDADO y la verificación se rehace determinista. Severidad: alta.
- [2026-06-20] MARCA/COLOR: la marca es NARANJA (home=fuente de verdad, 0 decorativo azul/morado/rojo/verde). CONSERVAR: `#22c55e`, `#25d366` (WhatsApp), logo Google en `<path fill>`, neutros slate. Off-brand → `#C2410C`/`#E36414`, fondos `#FFF7ED`. Aparece en HTML inline Y `styles*.css`, en HEX y en `rgb()/rgba()`. AUTO en `check-plantilla.py` check 12 (hex+rgb) + auto-fixer `color-off-brand`. CSS servido → bump `?v=`+sw.js. Casos color-blog/color-form-rgba-2026062x (HISTORIAL). Severidad: media.
- [2026-06-25] INFRA/AUTO-AGENTE: el driver (.pipeline/crecer-diario.sh) reintenta con backoff (3×, 120s→240s) y distingue corte de red (TRANSIENT_RE, reintenta) de límite de uso real (LIMIT_RE, corta); el motivo del correo se clasifica por OFFSET DE BYTES del intento (la línea de costo "📊 Uso (cuota)" se anexa después y NO cuenta como motivo). NO confundir "API Error: Connection closed" (corte de red, no facturado) con cuota agotada. Caso 20260624-1834 en HISTORIAL. Severidad: alta.
- [2026-06-26] COLONIA/CONTENIDO (AUTO en `check-plantilla.py` check 13): el `hero-eta-badge` ("Llegamos en NN-NN min a X") debe COINCIDIR con el ETA del cuerpo (meta+benefit-h3+cobertura); y la meta description NO debe quedar truncada a mitad de palabra antes de "· Llegada". Auto-sana: `python3 .pipeline/fix-colonia-eta.py --apply` (solo corrige el badge cuando las 3 fuentes del cuerpo coinciden — deriva, no inventa). Caso fix-colonia-eta-20260626 en HISTORIAL. Severidad: media.
- [2026-07-02] INFRA/RUTAS (AUTO en `check-rutas-pipeline.py`, auto-recogido por `check-infra.mjs`): constantes `os.path.join(ROOT,"archivo.ext")` que aún apuntan a la raíz pre-reorg en vez de `data/`/`scripts/` — reincidió 3× tras 5022f3bf. Caso infra-006/007 en HISTORIAL. Severidad: alta.
