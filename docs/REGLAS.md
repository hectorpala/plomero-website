# REGLAS APRENDIDAS — Plomero Culiacán

Reglas duras extraídas del historial de commits. Cada línea documenta un error real ya cometido para que un agente NO lo repita.

> PRESUPUESTO (se inyecta en CADA corrida — mantenlo curado): regla = 1-2 líneas (qué hacer + el checker que lo caza). El relato vive en HISTORIAL.jsonl, NO aquí. Mecanizado en checker → la regla se reduce a «qué + checker». Corre `python3 .pipeline/check-reglas.py` y consolida si excede.

- [2026-06-11] JS/MINIFICACIÓN: tras minificar main.js, verificar que las URLs `wa.me` no queden truncadas — una URL cortada genera un SyntaxError fatal que mata menú móvil, formularios y tracking en TODO el sitio. Severidad: alta.
- [2026-06-11] CACHÉ: al cambiar CSS **o JS (main.js)**, versionar la URL (`?v=AAAAMMDD`) en TODAS las páginas que lo referencian Y subir CACHE_NAME del service worker (sw.js) + versionar el asset en PRECACHE_ASSETS — ambos se sirven con Cache-Control immutable 1 año y el SW los precachea cache-first, así que sin versionar los visitantes recurrentes nunca reciben el fix. main.js estaba referenciado SIN `?v=` pese a estar la regla para CSS. Severidad: alta.
- [2026-06-11] MÓVIL: las tablas (precios, etc.) deben tener scroll horizontal contenido — patrón establecido: envolver `<table>` en `<div class="table-wrapper">` con `.table-wrapper{overflow-x:auto}` (regla en los 3 CSS); sin eso la columna derecha se sale del viewport en /, /precios/ y posts del blog. Severidad: alta.
- [2026-06-11] MÓVIL: toda imagen debe llevar `max-width:100%` (ej. .google-review-image, .before-after-image) — sin regla CSS renderizan a 1200px y desbordan en móvil. Severidad: media.
- [2026-06-11] MÓVIL: no usar grids con columnas fijas inline (ej. benefits-grid 2 columnas) — desbordan a 375px; usar `auto-fit minmax(260px,1fr)`. Severidad: media.
- [2026-06-11] MÓVIL/A11Y: los tap targets deben medir ~44px (breadcrumbs tenían 32x22px); ampliar área táctil con padding + margen negativo sin cambiar el aspecto visual. Severidad: media.
- [2026-06-11, consolidada 2026-06-19/2026-07-13/2026-07-14] CSS/PARIDAD: las 3 hojas (styles.css=fuente no servida; min/hash=servidas) deben tener las MISMAS reglas — `check-css-paridad.py`. Un fix que solo vive en un `<style>` inline por-página REGRESA al perder esa página su copia local (breadcrumb #E36414→#C2410C reincidió 3 veces, la última 2026-07-14) — centralizar SIEMPRE en las 3 hojas + bump ?v=/sw.js. Severidad: media.
- [2026-06-05] MÓVIL: los grid items que deben encogerse llevan `min-width:0`, no `auto` — con min-width:auto las columnas no se encogen y empujan contenido (.stats-grid) fuera de pantalla en ≤360px. Severidad: media.
- [2026-06-11] SEO: nunca crear cientos de páginas casi idénticas (colonias eran plantillas 80-86% iguales); consolidar en pocas páginas de zona con 301 — son doorways invisibles para Google y peso muerto de rastreo. Severidad: alta.
- [2026-06-09] SEO/SCHEMA: NO poner aggregateRating/review auto-servidos en blog ni páginas sin negocio real — viola la política self-serving reviews de Google; mantener reviews solo en páginas de negocio. Severidad: alta.
- [2026-06-08] SEO: verificar que `og:image` y `twitter:image` apunten a un archivo que EXISTE — apuntaban a archivos inexistentes en varias páginas. Severidad: media.
- [2026-06-11] ENLACES: al consolidar/eliminar páginas, revisar y reparar rutas de imagen inválidas (`../../../`) y enlaces internos rotos generados por el cambio de profundidad. Severidad: alta.
- [2026-06-11] IMÁGENES: no referenciar PNG inexistentes; usar los WebP que sí existen en el repo — había 18 rutas a PNG inexistentes. Severidad: media.
- [2026-06-11] CONTENIDO: el email de contacto correcto es `info@plomeroculiacanpro.mx`, NO `info@plomeropro.com` — estaba mal en 14 páginas. Severidad: media.
- [2026-06-11] SEO/ENLACES: al borrar o consolidar páginas, verificar CERO enlaces internos a las páginas borradas y actualizar sitemap (404, home, hubs, meta-refresh). Severidad: alta.
- [2026-06-11] SEO: las páginas off-topic (ej. marcha-paz) van con noindex y fuera del sitemap — no diluir el sitio temático con contenido ajeno. Severidad: baja.
- [2026-06-09] REDIRECTS: todo redirect debe apuntar a un destino que existe. Severidad: media.
- [2026-06-03] RENDIMIENTO/CLS (ampliada 2026-07-07): las imágenes (sobre todo el logo) deben declarar `width`/`height` para prevenir CLS, Y esos valores deben COINCIDIR con las dimensiones REALES del asset (un hero declaraba 1200x800 con asset servido de 800x800 → distorsión). Verificar con `sips -g pixelWidth -g pixelHeight <archivo>`. Severidad: alta.
- [2026-06-03] PWA: el `theme_color`/`theme-color` debe ser el color de marca (#F97316), no un placeholder (#0066cc), y debe existir en cada página. Severidad: baja.
- [2026-05-20] RENDIMIENTO: optimizar imágenes pesadas a WebP antes de servirlas — el logo pasó de 101KB a 6KB (-94%). Severidad: media.
- [2026-06-03] SEO: cada página indexable nueva debe quedar incluida en el sitemap (incluyendo priority_sitemap.xml) — faltaban páginas indexables. Severidad: media.
- [2026-06-12] SEO/SCHEMA: og:url y todas las URLs propias del JSON-LD (LocalBusiness url, items del BreadcrumbList) == canonical. BreadcrumbList de servicio = 3 niveles (Inicio→/, Servicios→/servicios/ real — nunca `BASE/#servicios` —, Página→canonical). AUTO en `check-indexabilidad.py` (parse-aware: el JSON-LD viene con y sin espacios, no comparar por string exacto). Severidad: media.
- [2026-06-12, ampliada] PERF/LCP: solo UNA imagen por página lleva `fetchpriority="high"` (el hero); el logo del nav y la 2ª imagen de contenido (bajo el fold, tras `<header class="hero">`) van con `loading="lazy"` sin fetchpriority — regla segura: cambiar eager→lazy solo en imgs SIN fetchpriority="high" y cuyo src no contenga "logo". Severidad: media.
- [2026-06-12] CSS/CASCADA: al añadir reglas al final de los CSS compartidos, verificar que no pisen propiedades de reglas anteriores de igual especificidad; medir con Chrome headless (display/getBoundingClientRect) en móvil Y desktop tras el cambio. Severidad: media.
- [2026-06-12, ampliada 2026-07-10] INFRA/NODE-PATH: `node` no está en el PATH por defecto en varios shells del pipeline (`/usr/local/bin` o `/opt/homebrew/bin` según sesión; su ausencia generó un FALSO "verificación ciega" en revisor-produccion) — anteponer siempre `export PATH="/opt/homebrew/bin:$PATH" &&` antes de `node ...`, nunca asumir que ya está en PATH. Severidad: baja (pero produce falsos positivos ALTA si no se detecta).
- [2026-06-13] A11Y/PLANTILLA: el modal exit-intent (`<div id="exit-intent-popup">`) debe llevar `role="dialog" aria-modal="true" aria-labelledby="exit-popup-title"` y su `<h3>` con `id="exit-popup-title"` — igual que index.html. AUTO en `check-plantilla.py` check 5. Severidad: media.
- [2026-06-14] MÓVIL/PLANTILLA: los botones flotantes (`.floating-btn`…) se estilan con CSS INLINE por página. Si el HTML usa `class="floating-btn"` pero el `<style>` NO trae las reglas, los CTA caen a position:static y <44px (rotos en móvil). Chequeo: `grep -c 'class="floating-btn'` ≥1 → `grep -c '.floating-btn{'` ≥1. Fix: copiar las 4 reglas de una página sana (p.ej. plomero-economico); inline → sin bump. Severidad: alta (CTA rotos).
- [2026-06-14] SEO/ENLACES: enlaces internos en forma de directorio (`/servicios/<slug>/`) == canonical, NUNCA `…/index.html` (ruta de rastreo duplicada; el checker de links no la caza porque sirve 200). Fix: `perl -i -pe 's{(href="\.?/servicios/[a-z-]+/)index\.html"}{$1"}g'`. Solo HTML→sin bump. Severidad: media.
- [2026-06-14] CONTENIDO/AÑO: el año caduco/desincronizado aparece donde el checker NO mira (title/h1 sí los cubre): tarjetas de "relacionados", badge "Actualizado", alt, FAQ, JSON-LD headline y CUERPO. Unificar TODO el año visible al del título real; `datePublished` NO se toca (fecha real), `dateModified`=hoy si editas. Severidad: media.
- [2026-06-16, ampliada 2026-07-08/09] MÓVIL/TAP-TARGET: un `<a>` de texto (prosa, tabla, footer, nav sin clase) rinde a la altura de línea (<44px) sin `display:inline-flex;align-items:center;min-height:44px` explícito — el padding no es transferible entre clases. Toda clase NUEVA hay que auditarla y medir TODAS sus instancias. Verificar headless 375px. Severidad: media.
- [2026-06-17] SEO/ENLACES: al CONSOLIDAR/borrar páginas, los enlaces rotos suelen estar en MÚLTIPLES formas en la MISMA página (lista absoluta + rejilla relativa); reescribir cada `href` al destino que EXISTE, nunca vía la URL redirigida (seo-401). Checker: `check-plantilla.py` (0 rotos, sin `href="./<slug-borrado>/"`). Severidad: alta.
- [2026-06-13] PROD/JS-RUNTIME: `node --check` valida solo SINTAXIS, no errores de RUNTIME que sí rompen en el navegador (caso prod-001: un `()` espurio invocando el id de setTimeout → TypeError en cada carga de /, /precios/, /contacto/). REGLA: validar el JS de producción cargando las páginas clave en Chrome headless y capturando `pageerror`/`console.error`. AUTOMATIZADO en `check-produccion.mjs` (revisor-produccion: + uptime 200, headers, mixed content, formulario). Severidad: alta.
- [2026-06-18] CRECIMIENTO/PLANTILLA: página de servicio NUEVA lleva `cuerpo_html` COMPLETO como las hermanas (FAQ, cta-box, CTA final, `#sobre-nosotros`, `#contacto`, cross-sell); wa.me/og:image/hero deben corresponder al servicio (sin fuga de otra zona); NO inventar testimonios. Fuga de zona: AUTO `check-plantilla.py`. Severidad: media.
- [2026-06-20] INFRA/SENSOR: el dead-man's switch `check-infra.mjs` dispara ALTA falsa "verificación ciega" si algo cambia sin actualizar el sensor. Un checker UTILIDAD (no sensor de página) declara `infra:utilidad-no-sensor` en su cabecera para auto-excluirse. Severidad: baja, pero envenena la confianza.
- [2026-06-19] BLOG/PLANTILLA (AUTO `check-plantilla.py`): sin `<div>` de bloque dentro de `<h2>`; sin `<section class="related-articles">` duplicada; `<header class="article-header">` NUNCA `aria-hidden` (solo `.article-title-hidden` interior va oculto); indexables con `og:locale`/`og:site_name`/`twitter:url`=canonical. Severidad: media.
- [2026-06-21] SISTEMA/VERIFICADOR: la FASE 7 debe ser SOLO-LECTURA (`subagent_type: verificador`, sin Edit/Write, git de solo-consulta) — JAMÁS `general-purpose`. Caso verifier-rogue: un verificador general-purpose implementó+commiteó+mergeó+pusheó a main sin autorización; su veredicto quedó INVALIDADO. Severidad: alta.
- [2026-06-20] MARCA/COLOR: la marca es NARANJA (home=fuente de verdad, 0 decorativo azul/morado/rojo/verde). CONSERVAR: `#22c55e`, `#25d366` (WhatsApp), logo Google en `<path fill>`, neutros slate. Off-brand → `#C2410C`/`#E36414`, fondos `#FFF7ED`. Aparece en HTML inline Y `styles*.css`, en HEX y en `rgb()/rgba()`. AUTO en `check-plantilla.py` check 12 (hex+rgb) + auto-fixer `color-off-brand`. CSS servido → bump `?v=`+sw.js. Severidad: media.
- [2026-06-25] INFRA/AUTO-AGENTE: el driver reintenta con backoff (3×, 120s→240s) distinguiendo corte de red (reintenta) de cuota agotada (corta) por OFFSET DE BYTES del intento, no por la línea de costo anexada después. Severidad: alta.
- [2026-06-26] COLONIA/CONTENIDO (AUTO en `check-plantilla.py` check 13): el `hero-eta-badge` ("Llegamos en NN-NN min a X") debe COINCIDIR con el ETA del cuerpo (meta+benefit-h3+cobertura); y la meta description NO debe quedar truncada a mitad de palabra antes de "· Llegada". Auto-sana: `python3 .pipeline/fix-colonia-eta.py --apply` (solo corrige el badge cuando las 3 fuentes del cuerpo coinciden — deriva, no inventa). Severidad: media.
- [2026-07-02] INFRA/RUTAS (AUTO en `check-rutas-pipeline.py`, auto-recogido por `check-infra.mjs`): constantes `os.path.join(ROOT,"archivo.ext")` que aún apuntan a la raíz pre-reorg en vez de `data/`/`scripts/`. Severidad: alta.
- [2026-07-07, ampliada 2026-07-09/2026-07-14] CONTENIDO/DATOS: garantía, precio, horario o `aggregateRating` no puede contradecirse entre el JSON-LD y el cuerpo, entre bloques del cuerpo, ni entre páginas hermanas del mismo negocio (recurrente: rating, precio, horario nocturno). Al unificar, usar el valor MAYORITARIO ya existente (deriva, no inventes). AUTO en `check-plantilla.py` check 15 (rating, site-wide; el resto es revisión LLM). Severidad: media.
- [2026-07-07, mecanizada 2026-07-09, con FIXER 2026-07-10, ampliada 2026-07-14] ENLACES: el texto del ancla y el `href` deben apuntar al MISMO destino FINAL (no un redirect-stub intermedio como `servicios/plomero/24-7/` en vez de `emergencia-24-7/`) — reincidió 4 veces. AUTO en `check-plantilla.py` check 14, PERO solo cuando el texto coincide con un H1 real de servicio; anclas con texto genérico (ej. "Servicio 24/7") que saltan a un redirect-stub NO se cazan aún — 10+ páginas comparten el mismo stub, sin mecanizar. Fix del href SIEMPRE anclado al span exacto `<a href>TEXTO</a>` (href+texto), nunca solo al href. Severidad: media.
- [2026-07-08] CONTENIDO/SCHEMA: un catálogo JSON-LD de varios `Service` embebido puede tener su `description` sobreescrita en bloque por error (todas las entidades con la del anfitrión). Cada `Service` lleva descripción PROPIA; garantía/precio debe coincidir con su página dedicada. Sin checker aún — revisar a mano el `@graph`. Severidad: alta.
- [2026-07-08] A11Y/COLOR: `.whatsapp-link` (`#25D366` sobre blanco, 1.98:1, falla AA) → usar `#075E54` (verde oscuro WhatsApp, 7.67:1) en TEXTO; verde brillante solo en iconos/botones. Severidad: alta.
- [2026-07-07, ampliada 2026-07-10] CSS/ALCANCE: un bloque de estilo compartido pensado para UNA página se aplica a CUALQUIER página con las mismas clases; si esa página trae scrim/fondo inline propio, debe fijar también el `color` dentro de su propio `@media` (si no, texto oscuro sobre fondo oscuro o viceversa — caso `.hero-rating` móvil 2026-07-10). REGLA: tras tocar una clase con overrides responsive, medir headless en AMBOS extremos (375px y ≥1024px); si el fix solo aplica a un breakpoint, escóplo con su propio `@media`. Severidad: alta.
- [2026-07-09] INFRA/CHECKERS: antes de excluir un directorio de `SKIP_DIRS` (o cualquier scope de checker) por "no se sirve", VERIFICARLO con `git ls-files | grep <dir>` + revisar `publish` en `netlify.toml` — casi se ciega el checker a `obsidian-vault/` (bitácora interna) que SÍ estaba trackeada y SÍ se servía en producción (Netlify `publish="."`). Bloqueado con `_redirects` (`404!` forzado) + `robots.txt Disallow`; confirmar con `curl -I` a producción tras publicar. Severidad: alta.
- [2026-06-11, ampliada 2026-07-13] SEO/GEO: página LOCAL (colonia/zona) necesita GPS real y ÚNICO — genérica repetida = doorway (no inventar sin geocodificar: pendiente humano, seo-107, en `plomero-zona-*`/`plomero-centro-culiacan`). Página de SERVICIO (mismo negocio toda la ciudad) es distinta: copia el geo/address REAL de la home — 12 páginas con placeholder compartido (24.8090556/-107.3940556) corregidas por deriva 2026-07-13. Clasificar por directorio antes de tocar geo en bloque. Severidad: media.
- [2026-07-14] A11Y/PERF (AUTO en `check-plantilla.py` checks 16-17): `<img>` con DOS atributos `alt` en la misma etiqueta (el navegador solo usa el primero, el segundo se pierde sin error) y `srcset`/`imagesrcset` con la misma URL repetida bajo anchos distintos (srcset falso, ningún archivo real de ese ancho) — 6 imágenes de `emergencia-24-7`. Severidad: media.
- [2026-07-14] SEO/SCHEMA (AUTO en `check-plantilla.py` check 18): JSON-LD `@id` == dominio raíz SIN fragmento (debe ser `.../#organization` o `.../#business`) es entidad huérfana o duplicada — no consolida reviews/rating en Google; mismo patrón en `Review.itemReviewed`. Confirmado en 9 páginas más además de la corregida hoy. Severidad: media.
- [2026-07-14] CONTENIDO: un conteo mencionado en el cuerpo (ej. "Ver todas las colonias (643)") puede quedar caduco frente al directorio real (24) tras una consolidación — sin checker aún; 5 páginas más pendientes (index, `plomero-cerca-de-mi`, `desazolve-de-drenajes`, `plomero-a-domicilio`, `plomero-economico`). Severidad: baja.
