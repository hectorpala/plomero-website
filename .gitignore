# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Windows
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
Desktop.ini

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json

# Logs
*.log
site-check/logs/

# Temp files
*.tmp
.tmp

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Python
*.pyc
__pycache__/
venv/
*.db

# Google APIs
client_secret.json
token.json
# Herramienta de backend: NO debe publicarse en la raíz web (evita exponer código
# y que un `netlify deploy --dir=.` suba gsc-token.json / client_secret.json).
mcp-local-seo/

# Build artifacts
dist/
build/

# Backup files
*.backup
*.backup-*
*.original

# Claude Code
.claude/settings.local.json

# Local Netlify folder
.netlify

# Reportes transitorios de Core Web Vitals (CI); la baseline (perf-baseline.json) SÍ se versiona
.pipeline/lighthouse-run-*.json
.pipeline/cwv-result.json

# Graphify: caché/salida generada. El grafo REAL se escribe FUERA del repo (~/graphify-plomero)
# para que su graph.html no lo escaneen los checkers (check-plantilla/links) ni lo toque el pre-push.
graphify-out/

# Vault privado de Obsidian — vive en disco, JAMAS en el repo (es publico) — 20-ago-2026
obsidian-vault/

# Backup de permisos locales de Claude Code (no debe publicarse)
plomero-settings.local.json.bak-*
